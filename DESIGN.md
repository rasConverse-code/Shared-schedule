# 课表搭子 — 设计规格

多人共享课表小程序。群友叠层查看谁被占用，点开看具体课程。

## 风格锚点

微信小程序工具类产品，靠近「系统日历 × 学生机票行程卡」的冷静实用感，不走营销页华丽风。界面本身是课表网格，不套装饰卡片。

## 色板

| Token | 值 | 用途 |
|-------|-----|------|
| `--ink` | `#1C1F26` | 主文字 |
| `--muted` | `#8B93A7` | 次要文字、时间轴 |
| `--paper` | `#F4F6FA` | 页面底 |
| `--card` | `#FFFFFF` | 面板、弹层 |
| `--line` | `#E8ECF4` | 网格线 |
| `--accent` | `#4F6EF7` | 主操作、当前用户 |
| `--accent-soft` | `#EEF1FE` | 主色浅底 |
| `--success` | `#22C55E` | 有空标记 |
| `--warn` | `#F59E0B` | 冲突/提醒 |

成员色（按加入顺序循环）：

1. `#4F6EF7` 靛蓝
2. `#22C55E` 绿
3. `#F59E0B` 琥珀
4. `#EC4899` 粉
5. `#06B6D4` 青
6. `#8B5CF6` 紫
7. `#EF4444` 红
8. `#14B8A6` 蓝绿

## 字体

系统栈：`-apple-system, BlinkMacSystemFont, "Helvetica Neue", "PingFang SC", "Microsoft YaHei", sans-serif`

| 角色 | 大小 | 字重 |
|------|------|------|
| 页标题 | 36rpx | 600 |
| 区块标题 | 30rpx | 600 |
| 正文/课程名 | 26–28rpx | 400–500 |
| 时间/节次 | 22rpx | 400 |
| 说明文字 | 22–24rpx | 400 |

## 布局

- 顶栏：群切换 + 周导航 + 成员头像条
- 主体：左侧节次轴 + 右侧 7 天网格（横向可滚动）
- 底部 Tab：课表 / 群组 / 我的
- 浮层：点格子弹出占用详情；右下 FAB 新增课程/事件

密度：每格高度约 96rpx，仅一人时显示课程短名；多人时显示头像点 + 人数，不堆文字。

## 数据模型

```
User     { _openid, nickName, avatarUrl, colorIndex }
Group    { _id, name, inviteCode, ownerId, memberIds[] }
Course   { _id, groupId, userId, type: 'course'|'event',
           title, location?, teacher?,
           dayOfWeek: 1-7,          // 周一=1
           startSlot, endSlot,      // 时段索引 1-6（含），对应 1-2/3-4/… 节
           startDate?, endDate?,    // 事件可选日期
           weeks?: number[],        // 课程生效周
           note? }
```

节次默认表（可配置）：

| 节次 | 时间 |
|------|------|
| 1–2 | 08:00–09:40 |
| 3–4 | 10:00–11:40 |
| 5–6 | 14:00–15:40 |
| 7–8 | 16:00–17:40 |
| 9–10 | 19:00–20:40 |
| 11–12 | 21:00–22:00 |

## 核心交互

1. **周视图叠层**：同格多人时，左侧色条按人叠加，右上角显示头像点与 `N人`。
2. **点开详情**：底部弹层列出该时间段每位成员的课程/事件（名称、地点、类型）。
3. **查空闲**：长按或点「查空闲」进入时段选择，高亮该格，列表显示谁有空/谁有课。
4. **分享进群**：群组页生成邀请码 / 转发小程序卡片，好友点开即加入。
5. **双轨录入**：课程（按周重复 + 节次）与事件（指定日期 + 节次）同一编辑表单切换。

## 签名元素

多人占用格：色条叠层 + 头像点阵。一人时安静显示课名；两人以上立刻变成「谁在忙」的信号灯，而不是把文字挤爆。

## 技术

- 微信小程序原生 + 云开发（数据库 / 云函数 / 分享）
- 未开通云环境时走本地 mock，界面可完整演示
- 云开发就绪后切换 `utils/store.js` 实现即可
